pipeline {
    
	agent any	
    stages{
        
        stage('git'){
            steps { 
		 git --version 

          }
         }
	    stage('maven'){
		    steps {
	                maven --version
         }
      }
    }
}
		    
                
